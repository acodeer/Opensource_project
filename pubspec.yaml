name: untitled
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  # ★ 1. SDK 제한을 Dart 3.x의 넓은 범위로 변경하여 최신 버전과의 호환성을 확보합니다.
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ★ 2. 주요 Firebase 패키지를 최신 안정화 버전(현재 시점 기준)으로 업데이트합니다.
  #    이는 하위 종속성(예: web-1.1.1) 충돌 문제를 해결하는 데 필수적입니다.
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_auth: ^5.1.0
  google_sign_in: ^6.2.1

  # 웹뷰 관련 패키지 버전 업데이트
  webview_flutter: ^4.13.0
  # webview_flutter_web: ^0.2.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0